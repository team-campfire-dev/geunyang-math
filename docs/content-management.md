# DB 콘텐츠 관리

2026-09-14 · [DB 콘텐츠 전환 #6](https://github.com/team-campfire-dev/geunyang-math/pull/6) 운영 반영 완료. 릴리스별 검증은 [배포 기록](deployment.md#릴리스별-검증-기록)을 따른다.

기본 콘텐츠와 이후 발행한 콘텐츠의 운영 원본은 MySQL이다. 앱은 `ClassVersion`, `DiagnosticVersion`, `TermVersion`, `Skill`을 읽는다. 클래스·진단 문항·개념 이름을 TypeScript/JSON 파일에서 불러오는 런타임 경로는 없다.

## 저장 구조

- `ClassVersion`: 클래스 판본. **읽기는 아래 행들에서 한다.** `metadata`가 클래스 자신에 대한 것(`public`과 숙제 문항 ID)을 담고, 섹션·블록·문항은 각자의 표에 있다. `document`는 같은 내용을 통째로 담은 불변 JSON으로 **여전히 기록하지만 읽지 않는다** — 되돌릴 때 이전 이미지가 찾는 자리이고, 다음 단계에서 없앤다.
- `DiagnosticVersion`: 진단 이름·설명·예상 시간·발행 시각과 불변 문항 JSON. `diagnosticKey=starting-point`의 최신 발행 판본을 새 진단에 사용한다.
- `TermVersion`: 용어 키, **용어를 가진 범위**(`scopeKind`·`scopeKey`), 용어가 속한 개념(`skillKey`), 표시 이름, 한 줄 요약, 설명 블록을 담은 불변 판본. 범위별·용어 키별 최신 발행 판본을 사용한다.
- `ClassSection`·`ContentBlock`: 발행된 클래스의 섹션과, 그 클래스가 가진 모든 블록(섹션 본문과 문항의 지문·힌트·해설). 카탈로그·수업 화면·초안 만들기가 모두 여기서 읽는다. `content:verify`는 이 행들을 도로 맞춰 본 결과가 `document`와 한 글자도 다르지 않은지 대조한다. 블록의 `fallback`은 문서에 **키가 없는** 것이지 비어 있는 것이 아니므로, 행에서는 NULL로 두고 되돌릴 때 키를 만들지 않는다. null로 되살리면 판본 해시가 달라진다.
- `PublishedProblem`: 발행된 판본이 가진 문항. 이름·순서와, 블록이 아닌 것(개념 키·응답 형식·채점 규칙·힌트 유무)을 행으로 갖는다. 문항을 읽을 때는 이 행과 `ContentBlock`을 합쳐 되돌린다. `document`에는 발행 당시의 문항 전체가 그대로 남아 있고 대조에만 쓴다 — 진단 문항의 블록은 아직 표로 옮기지 않았으므로 초안 저장의 문항 동일성 검사는 이 칸을 읽는다. 클래스·진단 문서가 여전히 원본이고, 이 표는 그 문서에서 파생한 사본이다. 초안이 「이 문항이 이미 발행된 적 있는가, 그때 내용은 무엇이었는가」를 물을 때 모든 발행 문서를 읽지 않게 한다. 판본을 쓰는 쪽이 색인도 같이 쓰고, 판본을 지우는 쪽이 색인도 같이 지운다 — `content:verify`가 문서와 색인이 어긋나면 배포를 세운다.
- `Skill`: 개념 키, 화면에 표시하는 이름, 표시 순서. 이름과 순서는 수정할 수 있다.
- `Enrollment.classVersionId`, `AssignmentItem.problemSnapshot`, `DiagnosticRun.document`: 수강·과제·진단의 기존 판본을 계속 참조하거나 복사해 보존한다.

JSON은 DB 안의 확장 가능한 블록 형식이다. `kind`, `typeVersion`, `payload`, `required`, `fallback` 계약은 유지한다. 기존 블록으로 만든 콘텐츠는 앱 재배포 없이 등록할 수 있다. 새로운 그래프·도형 블록 종류는 검증 스키마와 웹/모바일 렌더러를 추가한 뒤 배포해야 한다. 이번 변경은 편집 UI나 AI 생성기를 추가하지 않는다.

rich text는 `$...$`, `$$...$$`, `\(...\)` 안에 LaTeX를 담고, 도형 캡션은 한 줄로 표시하므로 인라인 형태만 쓴다. 수식 안에 한글을 넣으면 화면에 오류 표시가 나오므로 단위와 조사는 수식 밖에 둔다. 화면 낭독용 이름에는 수식 표기를 쓰지 않는다. `core.figure@1`은 `alt`가 그림 전체의 이름이고, `math.fraction_strip@1`은 수식 `label`을 쓸 때 평문 `labelAlt`를 함께 넣어야 한다. 섹션 제목은 평문만 담는다. 채점도 답안에서 같은 구분자와 정수로 이루어진 `\frac` 하나를 받으므로, 표기를 바꾸어도 기존 평문 답안은 그대로 통한다.

## 초기 설치와 기존 DB 업그레이드

`20260914030000_database_content` migration이 기본 3개 클래스(문항 15개), 진단 1개(문항 6개), 개념 3개를 한 번 등록한다. 클래스 판본이 이미 있으면 내용, 해시, 발행 시각을 그대로 둔다. 기존 진단 실행과 과제 snapshot을 변경하지 않는다. 테이블 및 UTF-8 SQL 문자열 비교에 기존과 동일한 `utf8mb4_unicode_ci`를 명시한다.

배포는 `db:migrate → content:publish → content:verify` 순서다. migration의 초기 데이터와 이미 적용한 번들은 다시 등록하지 않는다. `db:seed`는 이전 명령 호환용 읽기 전용 검증 별칭이다. migration SQL의 초기 데이터와 `tests/fixtures/initial-content.json`은 역사적 이전 자료·테스트 fixture이므로 운영 콘텐츠를 수정하는 곳이 아니다.

## 저장소 번들의 발행

`content/`의 JSON은 검토를 마친 **정답이 없는** 번들이다. 배포용 migrator 이미지가 이 디렉터리를 함께 담고, 기동 전에 `content:publish`로 파일 이름 순서대로 발행한다. 새 용어나 개념을 추가하려면 파일을 고쳐 머지하면 되고, 서버에서 따로 명령을 실행하지 않는다.

적용 여부는 migration과 같은 방식으로 판단한다. `AppliedContentBundle`이 파일 이름과 내용 checksum, 적용 시각을 보관하고, checksum이 같으면 DB 작업 없이 건너뛴다. 파일이 바뀌었을 때만 등록을 시도하며, 원장 기록은 등록과 같은 transaction에서 쓰므로 내용이 들어가지 않으면 적용으로 남지 않는다.

이미 발행한 `versionId`의 내용을 고쳐 커밋하면 등록이 거부되어 배포가 실패한다. 판본 불변 규칙을 배포 단계에서 강제하는 것이다. 수정은 새 `versionId`를 파일에 추가한다.

```sh
npm run content:publish -- --dry-run
npm run content:publish
npm run content:publish -- --dir /secure/path/reviewed
```

정답·채점 규칙·힌트·해설이 들어가는 클래스와 진단 번들은 이 디렉터리에 두지 않는다. 아래 수동 등록 경로를 쓴다.

## 자유롭게 그리는 그림

`core.scene@1`은 정해진 도형이 아니라 **좌표 평면 위의 도형 목록**이다. 분수 막대로 그릴 수 없는 설명 그림은 이 블록으로 그린다.

```json
{
  "blockId": "fraction-meaning:s1:b5",
  "kind": "core.scene",
  "typeVersion": 1,
  "required": true,
  "payload": {
    "alt": "피자 한 판을 넷으로 나누고 세 조각에 색을 칠한 그림",
    "width": 320,
    "height": 200,
    "items": [
      { "kind": "ellipse", "cx": 160, "cy": 100, "rx": 80, "ry": 80, "fill": "fill-soft", "stroke": "ink", "strokeWidth": 1 },
      { "kind": "path", "d": "M 160 100 L 160 20 A 80 80 0 0 1 240 100 Z", "fill": "fill" },
      { "kind": "line", "x1": 250, "y1": 60, "x2": 210, "y2": 80, "stroke": "ink", "arrow": "end" },
      { "kind": "text", "x": 255, "y": 58, "text": "한 조각", "size": 13, "fill": "ink" }
    ]
  }
}
```

도형은 `rect`·`ellipse`·`line`·`polygon`·`path`·`text`·`strip` 일곱 가지이고, 한 그림에 최대 120개까지 넣는다. `path`는 SVG path 문법을 그대로 받으므로 사실상 어떤 모양이든 그릴 수 있다. `strip`은 분수 막대 하나를 도형으로 담은 것이라 다른 도형과 나란히 놓을 수 있다. 공통 속성은 `fill`·`stroke`·`strokeWidth`·`dash`·`opacity`·`rotate`다.

색은 앱 팔레트 이름(`ink`, `green`, `fill`, `fill-soft`, `sand`, `sky`, `line`, `paper`, `white`, `none` 등)이나 `#rrggbb` 표기만 받는다. 이름을 쓰면 테마에 맞는 CSS 변수로 그려진다.

**마크업이 아니라 데이터다.** 값은 전부 렌더러가 만든 요소의 속성으로만 들어가고, 문서에 저자의 마크업이 그대로 삽입되는 경로는 없다. `path`의 `d`는 명령과 숫자만 허용하고, 알 수 없는 색 이름은 그려지지 않는다. 그래서 그림의 자유도를 열어도 임의 SVG를 붙이는 것과 달리 안전하다. 같은 이유로, 나중에 AI가 도형을 만들어 주는 기능을 붙이더라도 **생성 결과를 이 스키마로 검증**한 뒤 그대로 저장하면 된다.

그림 전체의 이름은 `alt`가 평문으로 맡는다. 도형 하나하나는 낭독에서 제외되므로, 그림이 말하려는 바를 `alt` 한 문장에 담는다. 캡션에는 수식을 써도 된다.

### 움직이는 그림

`frames`를 넣으면 같은 그림이 여러 장면으로 이어진다. 장면은 **이미 있는 도형을 옮기고 감추고 색을 바꿀 뿐**이고, 도형을 새로 만들거나 지우지 못한다. 그래서 정지 그림으로 안전한 것은 움직여도 안전하다.

```json
{
  "alt": "조각 하나가 막대 옆으로 옮겨 붙는 장면",
  "width": 320, "height": 200,
  "items": [
    { "id": "bar", "kind": "strip", "x": 20, "y": 80, "width": 280, "height": 40, "parts": 4, "filled": 3 },
    { "id": "piece", "kind": "rect", "x": 20, "y": 150, "width": 65, "height": 40, "fill": "sand" }
  ],
  "frames": [
    { "changes": [], "caption": "조각이 아직 따로 있어요." },
    { "changes": [{ "id": "piece", "dx": 215, "dy": -70 }], "caption": "네 번째 칸에 꼭 맞아요." }
  ],
  "frameMs": 1200
}
```

도형에 `id`를 주면 장면이 그 도형을 가리킬 수 있다. 장면의 변화는 `dx`·`dy`(이동), `opacity`, `rotate`, `fill`, `hidden`이며, 없는 이름을 가리키거나 이름이 겹치면 발행을 거부한다. 장면은 2~12개, `frameMs`는 400~6000이고 `loop`·`autoplay`를 함께 쓸 수 있다. 재생·멈춤·앞뒤 버튼은 항상 함께 그리며 `autoplay`는 동작 줄이기 설정에 양보한다. 장면마다 캡션을 따로 달 수 있고, 그림 전체의 이름은 여전히 `alt` 하나다.

편집기에서는 「움직이게 만들기」를 누르고 장면을 고른 뒤 도형을 끌면 그 장면에서의 위치가 정해진다. 「기본」을 고르고 끌면 그림 자체가 움직인다.

### 직접 놓아 보는 그림

`zones`와 `task`를 넣으면 학습자가 도형을 끌어다 놓는 그림이 된다. 끌 수 있는 도형은 `draggable: true`와 함께 `id`·`label`을 갖고, 자리는 `accepts`로 어떤 도형을 받을지 정한다(비우면 무엇이든 받는다).

```json
{
  "alt": "조각 두 개를 알맞은 칸에 놓는 그림",
  "width": 320, "height": 200,
  "items": [
    { "id": "half", "label": "반쪽 조각", "draggable": true, "kind": "rect", "x": 20, "y": 150, "width": 60, "height": 34, "fill": "sand" }
  ],
  "zones": [
    { "id": "slot1", "label": "첫째 칸", "x": 120, "y": 40, "width": 70, "height": 40, "accepts": ["half"] }
  ],
  "task": { "prompt": "조각을 알맞은 칸에 놓아 보세요.", "successText": "딱 맞았어요." }
}
```

조작 방법은 세 가지가 같은 일을 한다. 마우스로 끌어다 놓기, 도형을 눌러 든 뒤 자리를 누르기, 키보드로 도형에 초점을 두고 Enter를 눌러 든 뒤 자리에서 Enter. 그래서 드래그가 안 되는 기기에서도 전부 할 수 있다. 자리 하나에는 도형 하나만 들어가고, 다른 도형을 넣으면 먼저 있던 것이 제자리로 돌아간다.

**채점하지 않는다.** 놓은 결과는 서버로 가지 않고 진도·학습 증거·개인화에 반영되지 않으며 단계 완료를 막지도 않는다. 그래서 문항 본문·힌트·해설과 용어 풀이에 이 그림을 넣으면 발행을 거부한다. 같은 이유로 **한 그림은 스스로 움직이거나(`frames`) 학습자가 옮기거나(`zones`) 둘 중 하나**이고, 둘을 같이 쓰면 거부한다.

발행 검증은 이것들도 함께 본다. 자리가 있는데 `task`가 없거나, `task`만 있고 자리가 없거나, 끌 수 있는 도형이 하나도 없거나, 끌 수 있는 도형에 `label`이 없거나, 자리가 받겠다고 적은 도형을 옮길 수 없으면 거부한다.

편집은 `/authoring`의 캔버스에서 한다. 도형을 추가하고 마우스로 끌어 옮기고, 모서리 손잡이로 크기를 바꾸고, 색·선·회전·앞뒤 순서를 패널에서 고친다. `path`의 좌표와 다각형의 꼭짓점은 텍스트로 직접 적을 수도 있다.

## 화면에서 편집하고 발행하기

`/authoring`은 발행된 클래스를 기준으로 다음 판본의 초안을 만들고, 블록을 폼으로 고치고, 학습 화면과 같은 렌더러로 미리 보고, 검증을 통과하면 발행하는 화면이다. CLI와 같은 `importContent`를 호출하므로 검증·참조 검사·불변 규칙·트랜잭션이 모두 동일하다. 화면은 쓰기 경로를 하나 더 만드는 것이 아니라 같은 경로에 손잡이를 붙인 것이다.

### 권한

편집 권한은 **계정이 가진 역할**이다. 운영자 한 사람을 가정하지 않으므로, 이후 교사 시스템이 붙으면 같은 역할을 그 사람들에게 부여하면 된다.

| 역할 | 할 수 있는 일 |
|---|---|
| `admin` | 모든 초안 열람·편집·삭제, 발행 |
| `author` | 자기 초안 작성·편집·삭제, 검증. 발행은 못 함 |
| 없음 | 화면이 열리지 않음. 목록도 내려주지 않음 |

역할이 없으면 API가 403을 돌려주고 초안·클래스 목록을 아예 내려보내지 않는다. 아무에게도 역할을 주지 않은 배포에서는 이 화면이 누구에게도 열리지 않는다.

역할을 나눠 주는 일도 화면에서 한다. 초안 목록 아래 **「편집 권한」** 에서 관리자는 지금 권한을 가진 계정을 보고, 이름으로 계정을 찾아 `admin`/`author`를 주고 거둔다. `ContentAuthor` 행을 쓰는 것이라 **바로 반영되고 앱을 다시 띄울 필요가 없다.**

권한을 잃게 하는 조작에는 관문이 둘 있다. **자기 관리자 역할은 스스로 내려놓을 수 없고**, **마지막 관리자 행은 거둘 수도 `author`로 낮출 수도 없다.** 역할을 낮추는 것과 거두는 것은 같은 상실이므로 같은 관문을 지난다. 앞의 것만으로도 관리자가 0명이 되는 일은 막히지만, 뒤의 것은 환경 변수로 지정된 관리자가 배포와 함께 사라질 마지막 행을 없애는 경우를 위한 것이다.

- 첫 관리자는 `CONTENT_ADMIN_SUBJECTS` 환경 변수에 Google `sub`를 쉼표로 나열해 지정한다. 아직 아무 행도 없는 새 배포에 권한을 줄 사람이 최소 한 명 있어야 하기 때문이다. **부트스트랩 전용**이며, 그 계정으로 화면에서 자신을 포함해 역할을 나눠 준 뒤에는 변수를 비워도 된다.
- 환경 변수로 지정된 관리자는 행이 아니므로 화면이 「배포 설정」으로 표시만 하고 거두지 않는다. 그 역할은 배포를 고쳐야 바뀐다.
- 공용 `open-authoring` 계정에는 역할을 줄 수 없다. 아무도 로그인하지 않아서 생긴 계정이지 권한을 맡길 사람이 아니다.
- 계정 찾기는 표시 이름의 일부로 찾는다. 교사 권한 모델이 생기면 같은 행을 쓰거나 같은 함수에 조직 규칙을 더하면 되고, 화면과 API는 고치지 않는다.

### 로그인 없이 여는 스위치

`CONTENT_OPEN_ACCESS=true`면 `/authoring`이 로그인도 역할도 없이 열리고, 방문자는 공용 `open-authoring` 계정으로 편집·발행한다. 아직 사용자가 없는 배포에서 혼자 콘텐츠를 만들 때를 위한 스위치다.

이것은 접근 제어가 아니다. 켜져 있는 동안에는 **경로를 알아낸 누구든 발행·삭제하고 미발행 초안을 읽을 수 있다.** 주소를 아무에게도 알리지 않았다는 것은 추측 난이도이지 권한 검사가 아니므로, 서비스를 공개하기 전에 끄고 역할 기반 권한으로 돌아간다. 기본값은 꺼짐이고, 켜진 상태에서 첫 요청이 오면 서버 로그에 `content_authoring_open`을 한 번 남긴다. 켜고 끄는 것은 그때까지의 발행 기록에 영향을 주지 않는다.

### 문항 편집

문항은 그것이 속한 활동에서 쓰고 고친다. 「문항 묶음」 블록을 열면 그 활동이 가진 문항이 차례로 있고, 각 문항에서 **문제 본문·정답·다루는 개념·힌트·해설**을 고친다. 본문·힌트·해설은 수업 본문과 같은 블록 카드를 쓰므로 그림이나 용어 풀이도 문항 안에 넣을 수 있다. 문항 묶음 안에 또 문항 묶음을 넣는 것만 막혀 있다.

정답은 **학습자가 입력할 형태 그대로** 적는다. `3`처럼 정수로 적으면 정수로 답하는 문항이 되고, `1/2`이나 `0.5`처럼 적으면 값이 같은 답을 모두 정답으로 본다. 분수로 적었을 때만 「기약분수로 써야 정답」을 켤 수 있다. 읽어 들인 답은 가장 작은 같은 값으로 저장하므로 `2/4`로 적으면 `1/2`로 되돌아온다. 응답 형식(`responseSpec`)과 힌트 유무(`hintAvailable`)는 정답과 힌트에서 따라오므로 따로 적지 않는다. 편집 화면은 이 둘을 서버로 보내지도 않는다.

**문항도 불변이다.** 이미 발행된 판본이 가진 문항의 내용을 고치면 저장할 때 **새 `problemVersionId`** 가 만들어지고, 그 문항을 가리키던 활동과 숙제 참조가 함께 옮겨간다. 이름은 판본을 따라간다 — `practice-1:v1`을 `:v2` 초안에서 고치면 `practice-1:v2`가 된다. 아직 발행된 적 없는 문항(같은 초안에서 방금 쓴 것)은 이름이 그대로다. 학습자의 풀이·힌트 기록과 과제 snapshot이 모두 이 이름을 가리키므로, 한 번 답이 제출된 이름이 다른 문항을 뜻하게 둘 수는 없다.

문항을 빼면 **쓰고 있는 판본에서만** 빠진다. 이미 발행된 판본은 자기가 발행될 때의 문항을 그대로 가지고 있다.

초안은 기준 판본의 문서를 복사해 만든다. 문항을 쓴다는 것은 정답을 쓴다는 것이므로, **정답·힌트·해설은 콘텐츠 역할을 가진 계정에만 내려간다.** 학습 API는 예전과 같이 학습자에게 공개 절반만 보낸다.

### 용어 편집

용어도 화면에서 쓴다. 초안 목록 아래 **「용어 풀이」** 에서 범위를 **공통 사전 / 클래스 용어** 중에 고르고, 그 범위의 용어를 불러와 고치거나 새로 쓴다. 공통 사전은 관리자만, 클래스 용어는 콘텐츠 역할이 있으면 쓸 수 있다 — 클래스 용어를 쓰는 것은 그 클래스를 쓰는 일의 일부이기 때문이다.

**용어는 초안을 거치지 않는다.** 저장이 곧 다음 판본의 발행이고, 그 용어를 링크한 모든 수업에 바로 반영된다. 이것은 용어가 원래부터 하던 약속이다 — 클래스는 용어를 키로 링크하므로, 문구를 고치는 데 클래스를 다시 발행할 필요가 없다. 이전 판본은 그대로 남는다.

판본 이름은 범위가 정한다. 공통 사전은 `term.denominator:v2`, 클래스 용어는 `fraction-meaning:term.denominator:v2`. 설명 블록의 ID도 그 판본 이름을 따라 서버가 붙이므로 작성자가 고를 일이 없다.

발행한 뒤에는 **용어 키와 다루는 개념을 바꿀 수 없다.** 키는 본문이 부르는 이름이고, 개념은 노출 판단의 근거라 판본마다 달라지면 이미 발행된 클래스들의 노출이 소리 없이 바뀐다. 발행은 CLI와 같은 `importContent`를 지나므로, 없는 개념을 가리키거나 참조를 깨뜨리는 정의는 여기서도 같은 이유로 거부된다.

설명 블록에는 문항 묶음도, 또 다른 용어 주석도, 놓아 보는 그림도 넣을 수 없다. 풀이는 문항이 기다리는 동안 읽는 설명이라 조작을 요구하지 않는다.

### 이 화면이 고치는 범위

그래서 지금 이 화면에서 **할 수 없는 일**도 분명하다. 새 클래스를 처음부터 만들기, 진단 편집, 클래스가 가르치는 개념 목록 자체를 고치는 일은 아직 CLI 경로를 쓴다. 조직·코스 범위의 용어도 모델에는 있지만 화면에는 없다. 문항이 고를 수 있는 개념은 그 클래스가 이미 가르친다고 적어 둔 개념뿐이다.

### 절차

1. 클래스를 고르고 **초안 만들기**. 판본 ID는 `classKey:v{다음 번호}`로 제안되며 직접 고칠 수 있다.
2. 단계를 고르고 블록을 추가·수정·이동·삭제한다. 블록 종류마다 전용 폼이 있고, 오른쪽 미리보기는 학습 화면과 같은 렌더러로 그린다.
3. **저장** 후 **검증**. 검증은 발행과 같은 dry run이라 참조 오류·중복 ID·낭독용 이름 누락까지 여기서 드러난다.
4. **발행**. 되돌릴 수 없으므로 확인 단계를 한 번 거친다. 발행하면 `ClassVersion` 행이 새로 생기고 초안은 잠긴다.

작성 중인 초안은 틀린 상태로도 저장된다. 검증을 통과해야 발행 버튼이 열리고, 이미 발행된 판본 ID로 발행하면 CLI와 같은 불변 오류로 거부한다. 발행한 초안은 더 고칠 수 없고, 다음 수정은 새 초안으로 만든다.

기존 수강자는 시작한 판본을 그대로 유지한다. 새 판본은 새로 수강하는 사람부터 보인다. 새 종류의 블록을 넣었다면 그 블록을 아는 앱이 먼저 배포되어 있어야 한다.

## 콘텐츠 내보내기·등록

검토된 DB 접속 환경의 서버/관리자 터미널에서 실행한다. `DATABASE_URL`은 해당 DB로 설정한다. 명령은 지정된 DB를 대상으로 하며 운영용 비밀값은 셸 기록이나 저장소에 적지 않는다.

```sh
npm run content:verify
npm run content:export -- --out /secure/path/content-export.json
npm run content:import -- --file /secure/path/reviewed-content.json --dry-run
npm run content:import -- --file /secure/path/reviewed-content.json
npm run content:verify
```

내보내기에는 정답·채점 규칙·힌트·해설이 포함된다. 파일은 `0600` 권한으로 새로 만들며 기존 파일을 덮어쓰지 않는다. 공개 API나 모바일 번들에 포함하지 않는다. 등록·검증 결과는 개수만 출력한다. 웹 사용자에게 등록 API를 제공하지 않는다.

번들 형식은 아래와 같다. 내보낸 파일을 복사해 편집하면 각 문서의 전체 형식을 확인할 수 있다.

```json
{
  "schemaVersion": 1,
  "skills": [],
  "classes": [],
  "diagnostics": [],
  "terms": []
}
```

`terms`는 나중에 추가한 항목이라 생략할 수 있다. 이전에 내보낸 번들 파일도 그대로 다시 등록된다.

필요한 항목만 넣는 부분 등록도 지원한다. 예를 들어 기존 개념을 참조하는 새 클래스만 넣고 `skills`와 `diagnostics`는 빈 배열로 두어도 된다. 개념 키는 기존 DB 또는 같은 번들에 반드시 있어야 한다. 파일 크기는 최대 5 MiB이며 큰 번들은 나누어 등록한다.

발행한 클래스/진단은 덮어쓰지 않는다. 수정하려면 `versionId`를 새로 부여한다. 내용이 바뀐 문항도 `problemVersionId`를 새로 부여하고 섹션·숙제 참조를 함께 변경한다. 클래스·진단의 같은 판본 ID에 동일 내용을 재등록하면 기존 판본을 변경하지 않는다. `Skill`은 같은 키로 이름·표시 순서를 갱신할 수 있다. MySQL이 JSON 객체 키 순서를 바꾸어도 의미가 같으면 동일한 내용으로 판단한다. 기존 해시는 다시 계산하지 않으며 새 판본 해시는 정렬된 JSON 키를 기준으로 계산한다.

새 클래스/진단 판본은 번들 배열 순서대로 발행된다. 같은 classKey/diagnosticKey를 가진 새 판본 중 마지막 항목이 최신이 된다. 내보내기도 발행 순서로 제공한다. 이미 있는 판본 재등록은 최신 판본을 되돌리지 않는다. 이전 내용으로 되돌리고 싶다면 새 판본 ID로 재발행한다.

등록 전 전체 참조와 문서 형식을 검사한다. 알 수 없는 필수 블록, 중복 ID, 없는 개념, 바뀐 기존 판본, 기존 문제 ID의 다른 내용, 클래스와 진단에 겹치는 문제 ID, 수식 `label`에 빠진 `labelAlt`, 수식 표기가 섞인 낭독용 이름은 거부한다. 용어에서는 없는 참조, 없는 `skillKey`, 판본마다 달라지는 용어의 개념(범위별로 센다), 본문에 없는 표층 문자열, 자신이 평가하는 개념을 문항 안에서 설명하는 주석, 다른 클래스가 가진 용어를 가리키는 참조, 범위를 적고도 어디 것인지 밝히지 않은 용어를 거부한다. 쓰기는 한 DB 트랜잭션으로 처리하며 `--dry-run`은 쓰지 않는다.

## 용어 풀이와 진행도

본문에서 수학 용어에 설명을 달려면 `core.rich_text@2` 블록을 쓴다. 편집기에서는 본문을 쓰다가 **`@` 뒤에 이름을 적으면** 걸 수 있는 용어가 떠오르고, 고르면 그 이름이 본문에 들어가면서 연결이 함께 적힌다. 같은 낱말이 여러 번 나올 때 **몇 번째인지는 적은 자리가 정한다** — 작성자가 세지 않는다. 한 블록에 한 용어는 한 번만 걸 수 있으므로, 같은 용어를 다시 고르면 연결이 그 자리로 옮겨간다. 목록에서 직접 고쳐도 되고, 고를 수 있는 용어는 공통 사전과 그 클래스가 가진 것뿐이다. 정의는 본문에 넣지 않고 `TermVersion`을 참조한다. 정의를 고칠 때 그 용어를 쓰는 클래스를 모두 재발행하지 않기 위해서다.

### 용어를 가진 범위

용어는 **누가 가지고 있는지**로 나뉜다. 같은 키라도 범위가 다르면 서로 다른 용어이고, 판본과 개념 이력도 따로 센다.

| 범위 | 누가 발행하나 | 지금 |
|---|---|---|
| `global` | 운영자. 수학 사전에서 모은 공통 용어 | 쓰고 있다 |
| `organization` | 학원 같은 조직 | 자리만 있다 |
| `course` | 코스 | 자리만 있다 |
| `class` | 그 클래스의 작성자 | 쓰고 있다 |

**본문이 용어를 가리킬 때 범위까지 적는다.** 주석은 `{ termKey, surface, scopeKind?, scopeKey? }`이고, 범위를 적지 않으면 공통 사전을 뜻한다. 읽는 쪽이 「이 문항이 어느 클래스·조직의 것인가」를 몰라도 정의를 찾을 수 있어야 하기 때문이다 — 과제는 여러 클래스의 문항을 섞어 담으므로, 읽는 시점에는 그 맥락이 없다.

편집 화면에서는 「어디 용어」를 **공통 사전 / 이 클래스** 중에서 고른다. `scopeKey`(어느 클래스인지)는 블록이 들어 있는 문서에서 따라오므로 저장할 때 서버가 채운다. 그래서 **한 클래스는 자기 용어만 가리킬 수 있고**, 다른 클래스의 용어를 참조하면 발행이 거부된다.

범위가 생겨도 **보일지 말지는 그대로 개념(`skillKey`)이 정한다.** 클래스가 가진 용어라고 해서 그 클래스에서 무조건 보이는 것이 아니라, 아래 표의 규칙을 똑같이 따른다.

```json
{
  "blockId": "fraction-addition:s1:b1",
  "kind": "core.rich_text",
  "typeVersion": 2,
  "required": true,
  "payload": {
    "text": "분모가 다르면 동치분수로 바꾸어 분모를 같게 만들어요.",
    "terms": [
      { "termKey": "term.denominator", "surface": "분모" },
      { "termKey": "term.equivalent-fraction", "surface": "동치분수" }
    ]
  }
}
```

`surface`는 본문에 실제로 있는 문자열이고, 같은 말이 여러 번 나오면 `occurrence`로 몇 번째를 가리킬지 정한다(기본값 1). 수식 구간 안의 글자는 세지도, 표시하지도 않는다. 조사가 붙은 "분모를"에서도 `surface`가 "분모"면 그 부분만 표시한다.

**보여줄지는 수업을 쓴 사람이 정한다.** 어떤 낱말에 용어를 걸었다는 것이 곧 「여기서는 설명해도 된다」는 판단이므로, 서버는 학습자의 진도를 보고 그 판단을 뒤집지 않는다. 건 것은 전부 내려가고, 걸지 않은 낱말은 평문으로 남는다.

한때는 서버가 진도로 정했다 — 지금 배우는 클래스가 가르치는 개념이나 과제가 평가하는 개념의 용어를 숨기고, 선수 개념과 앞 순서 클래스의 것만 내려보냈다. 규칙이 옳게 도는지 확인하려면 클래스 순서·선수 개념·과제 구성까지 머릿속에 들고 있어야 해서, 정작 글을 쓰는 사람이 결과를 예측하기 어려웠다. 그래서 규칙을 걷어내고 판단을 작성자에게 돌려주었다.

남아 있는 제약은 하나다. **문항 안에서 그 문항이 평가하는 개념을 주석하면 발행 단계에서 거부한다.** 이것은 진도가 아니라 답이 답 옆에 놓이는 문제라서 그대로 둔다. 같은 개념이라도 섹션 본문에서는 얼마든지 걸 수 있다.

진단 문항에는 용어 풀이를 제공하지 않는다. 이미 배운 개념 중 아직 연습이 필요한 것은 더 눈에 띄게 표시한다.

용어 정의는 클래스·진단과 달리 **항상 최신 판본을 조회해 보여준다**. 늦게 묶는 것은 판본뿐이고, **어느 용어인지는 주석을 쓸 때 정해진다**. 이미 시작한 클래스 판본과 과제 snapshot의 본문에서도 마찬가지다. 채점과 무관한 보조 설명이므로 판본을 고정하는 대신 정정이 즉시 반영되는 쪽을 택했다. 정의를 고치려면 새 `versionId`로 같은 `termKey`를 다시 발행한다. 기존 판본은 그대로 남는다.

용어 열람은 힌트와 다르다. 도움받은 풀이로 기록하지 않으며 개인화 판단에도 사용하지 않는다.

### 기본 용어 등록과 기존 클래스 주석

저장소의 `content/glossary-v1.json`은 정답이 없는 용어 전용 번들이라 그대로 등록할 수 있다.

```sh
npm run content:import -- --file content/glossary-v1.json --dry-run
npm run content:import -- --file content/glossary-v1.json
```

기존 클래스 본문에 주석을 다는 것은 별도 작업이다. 발행한 클래스는 불변이므로 `content:export`로 내려받아 해당 `core.rich_text` 블록을 위 형식으로 바꾸고, `public.versionId`를 새로 부여해 등록한다. 문항을 바꾸지 않으면 `problemVersionId`는 그대로 재사용한다. 기존 수강자는 이전 판본을 유지하고 새로 수강하는 사람부터 주석본을 받는다. 내보낸 파일에는 정답이 들어 있으므로 저장소에 커밋하지 않는다.

2026-09-15에 이 절차로 수식과 용어 주석을 담은 `v4` 판본 3개를 발행했다. 실행 기록은 [배포 문서](deployment.md)에 있다. 그 판본들은 서버가 진도로 노출을 정하던 때에 쓰였으므로, 자기 개념이라 어차피 숨겨질 낱말에는 주석을 달지 않았다. 규칙이 사라진 지금은 건 것이 모두 보이며, 각 클래스가 자기 개념의 낱말도 설명하려면 그 판본을 다시 발행해 주석을 더하면 된다.

## 물러난 블록

분수 막대만 그릴 수 있던 블록들은 새 콘텐츠에서 쓰지 않는다. 그림은 `core.scene@1` 하나로 그리고, 분수 막대는 그 안의 `strip` 도형으로 넣는다.

| 블록 | 상태 |
|---|---|
| `core.figure@1` | 발행된 판본이 계속 그려진다. 편집기에서 열어 고칠 수 있지만 새로 추가할 수는 없다. |
| `math.fraction_strip@1` | 위와 같다. |
| `math.fraction_sequence@1` | 완전히 제거했다. 발행된 콘텐츠가 없었다. 움직임은 `core.scene@1`의 `frames`로 만든다. |
| `math.fraction_builder@1` | 완전히 제거했다. 발행된 콘텐츠가 없었다. |

발행된 판본은 불변이라 고칠 수 없으므로, 이미 그 블록을 담은 클래스가 있는 한 검증 스키마와 렌더러는 남긴다. 스키마를 지우면 그 판본을 수강 중인 사람의 수업이 열리지 않는다. 편집기 팔레트에서 빼는 것과 앱에서 지우는 것은 다른 일이다.

## 학습 중 콘텐츠가 추가되면

새로 수강하는 사람에게 최신 클래스가 제공된다. 이미 수강한 사람은 시작했던 클래스 판본을 유지한다. 배정된 과제와 저장한 답안도 그대로다. 시작점 확인의 안내·문항 수·예상 시간은 DB에서 읽고, 시작한 진단은 그때 복사한 문항으로 끝까지 이어간다. 진단 개편만으로 진행 중인 진단을 교체하지 않는다. 완료 후 재진단을 안내하는 별도 UX는 후속 범위다.

## 검증

`tests/content-store.test.ts`에서 MySQL 초기 데이터, 내보내기 재등록, dry run, DB만으로 새 클래스·개념·진단 제공, 불변 판본·문항 보호, 과제와 진단 snapshot 보존을 검사한다. 용어는 발행·불변 판본·없는 참조 거부와, 클래스를 재발행하지 않고 정의를 고쳐 쓰는 경로를 함께 검사한다. 범위가 생긴 뒤로는 같은 키를 가진 사전 용어와 클래스 용어가 서로를 대신하지 않는 것, 각 범위가 자기 개념 이력을 지키는 것, 다른 클래스의 용어를 가리키면 거부하는 것도 같은 파일에서 본다.

편집 화면 쪽은 `tests/authoring.test.ts`(블록 폼·이름 짓기·정답 읽기·범위 채우기)와 `tests/authoring-integration.test.ts`(MySQL에서 초안·문항 개명·역할 부여·용어 발행)가 맡는다. `@`로 용어를 거는 계산은 `tests/term-mentions.test.ts`가 보고, 그 결과를 다시 `locateTerms`에 넣어 같은 자리를 가리키는지까지 확인한다. 본문 표시 위치 계산은 `tests/rich-text.test.ts`, 클래스·과제 응답에 실제로 실리는 용어는 `tests/learning-integration.test.ts`에서 본다. 기존 채점·개인화·인증 테스트도 함께 실행한다. 전체 320개 테스트·타입 검사·웹/모바일 빌드를 통과했다. 로컬에서 이전 릴리스의 DB와 학습 기록을 생성한 뒤 새 migration을 적용해 기존 모든 행과 해시·시각·snapshot이 동일함을 확인했다. 운영에서도 migration·배포 성공과 공개 클래스 3개·블록·15문항의 이전 내용 일치를 확인했다. 이 검증은 수학 콘텐츠의 전문가 검수를 대신하지 않는다.
